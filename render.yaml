services:
  - type: web
    name: movies-download-api
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8000
      - key: AUTH_ENABLED
        value: "false"
      - key: API_KEY
        sync: false # Set this manually in Render dashboard for security
